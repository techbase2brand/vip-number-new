import React from "react";
import ProductDeal from "./ProductDeal";

export const metadata = {
  title: "Deals - VIP Number Shop | Exclusive Mobile Number Offers",
  description: "Discover amazing deals on VIP mobile numbers at VIP Number Shop. Get premium numbers at discounted prices with exclusive offers. Limited time deals on numerology numbers, business numbers, and lucky numbers.",
};

const page = () => {
  return <ProductDeal />;
};

export default page;
